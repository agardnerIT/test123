{
    "active": "{{ .clusterActive }}",
    "label": "{{ .name }}",
    "endpointUrl": "{{ .clusterEndpoint }}",
    "eventsIntegrationEnabled": "{{ .eventsActive }}",
    "workloadIntegrationEnabled": "{{ .workloadIntegrationEnabled}}",
    "certificateCheckEnabled": "{{ .certCheckEnabled }}"
}